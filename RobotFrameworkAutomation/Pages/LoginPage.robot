*** Settings ***
Library    SeleniumLibrary


*** Keywords ***
User enter username
    [Arguments]        ${Username}
    input text         xpath=//sdsdsdf        ${Username}

User enter password
    [Arguments]        ${Username}
    input text         xpath=//sdsdsdf        ${Username}